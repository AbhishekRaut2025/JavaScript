let h1 = document.querySelector("h1");          //querySelector is to select the html element
console.dir(h1);
h1.textContent = "Hi Abhishek";                 // chenged th orignal content
h1.innerHTML = "<i>Hey</>"                      // changed tag/html using innerHTML key
h1.hidden = true;                               // hides element from web page